# 安全问题 / Security

## 报告方式

**不要开公开 issue。** 请发邮件到 <zhaojia789456@gmail.com>，标题带上 `[briffy security]`。

请写清楚：受影响的版本、复现步骤、你认为的影响面。有 PoC 最好，但别把真实的个人
数据放进去。

我会在 **72 小时内**回一封确认信。修好之后会在 release note 里说明问题和影响，
并按你的意愿署名或匿名。这是个人项目，没有赏金。

## 支持的版本

只有最新的一个 release 会收到修复。

## 这个项目的攻击面

briffy 读的是屏幕、麦克风、剪贴板和用户拖进来的文件——一台机器上最敏感的四样东西。
以下几类问题请优先报告：

| | |
| --- | --- |
| **数据外泄** | 任何让工作区内容（截图、录音、转写、剪贴板）离开本机、而用户没有明确配置过的路径 |
| **Electron 越权** | 渲染进程拿到 Node 能力、`contextIsolation` 被绕过、preload 暴露的 IPC 被滥用、外部内容在应用窗口里被加载 |
| **浏览器扩展** | 扩展有 `<all_urls>` 权限和一个 MAIN world 的注入脚本（`extension/hook.js`）。任何让网页反过来驱动扩展、或让扩展把非用户勾选的内容发进工作区的路径 |
| **本地 HTTP 桥** | 扩展和应用之间走 `127.0.0.1` 上的一个本地端口。任何让别的本地程序或网页往里塞东西的路径 |
| **模型与文件解析** | OCR / 语音 / PDF 解析吃的是不可信输入，解析器崩溃或被利用都算 |
| **密钥处理** | API Key 存在系统密钥库（Windows DPAPI / macOS Keychain）。任何把它写进明文、日志或渲染进程的路径 |

## 不算问题的

- 需要攻击者已经拿到用户账号本地权限才能做到的事——那时工作区目录本来就是他的。
- 用户自己配置的第三方 AI 服务（Anthropic / OpenRouter / Ollama / 自定义接口）把数据
  发出去。这是那个配置本身的意思，[docs/PRIVACY.md](docs/PRIVACY.md) 里写明了。
- 未签名的 fork 构建触发 Gatekeeper 警告。
